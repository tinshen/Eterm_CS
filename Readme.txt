1、程序启动后每隔五分钟检查一下是否有运行计划需要后自行(通过timer1来检查目前处理的JOB是否有计划需要运行)
2、如果有计划且计划的类型是GDS，则执行button1_Click方法去取订座数据。
   启动后直接点击“订座系统”按钮，也可以执行button1_Click方法去取订座数据。
3、button1_Click方法从p_ftp_deal_record a,p_ftp_down_config b中获取需要执行的计划项，从而得到指令集合以及查询订座数据之后写入的文件名和文件路径。
如果有需要处理的文件，则启动timer4来处理
4、timer4中每次判断